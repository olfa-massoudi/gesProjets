{% extends 'base.html.twig' %}

{% block title %}Commantaire index{% endblock %}

{% block body %}
    <h1>Commantaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_comm</th>
                <th>Utlisateur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commantaire in commantaires %}
            <tr>
                <td>{{ commantaire.id }}</td>
                <td>{{ commantaire.dateComm ? commantaire.dateComm|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ commantaire.utlisateur }}</td>
                <td>
                    <a href="{{ path('commantaire_show', {'id': commantaire.id}) }}">show</a>
                    <a href="{{ path('commantaire_edit', {'id': commantaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commantaire_new') }}">Create new</a>
{% endblock %}
